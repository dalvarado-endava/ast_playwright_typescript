export enum LoginError {
  EMAIL,
  PASSWORD,
  AUTHENTICATION,
}

export enum RegisterError {
  PHONE,
  LASTNAME,
  FIRSTNAME,
  PASSWORD,
  ADDRESS,
  CITY,
  ZIPCODE,
  STATE,
  COUNTRY,
}

export enum NavbarButton {
  SEARCHBUTTON,
  CONTACT,
  SIGNIN,
  WOMEN_TAB,
  DRESSES_TAB,
  TSHIRTS_TAB,
  CART,
}
