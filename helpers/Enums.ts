export enum LoginError {
  EMAIL,
  PASSWORD,
  AUTHENTICATION,
}

export enum RegisterError {
  PHONE,
  LASTNAME,
  FIRSTNAME,
  PASSWORD,
  ADDRESS,
  CITY,
  ZIPCODE,
  STATE,
}

export enum NavbarButton {
  SEARCHBUTTON,
  CONTACT,
  SIGNIN,
  WOMEN_TAB,
  DRESSES_TAB,
  TSHIRTS_TAB,
  CART,
}
