export enum LoginError {
  EMAIL,
  PASSWORD,
  AUTHENTICATION,
}

export enum RegisterError {
  PHONE,
  LASTNAME,
  FIRSTNAME,
  PASSWORD,
  ADDRESS,
  CITY,
  ZIPCODE,
  STATE,
  COUNTRY,
  ALIAS,
}

export enum NavbarButton {
  SEARCHBUTTON,
  CONTACT,
  SIGNIN,
  WOMEN_TAB,
  DRESSES_TAB,
  TSHIRTS_TAB,
  CART,
}

export enum AccountButton {
  ORDER_DETAILS,
  CREDIT_SLIPS,
  ADDRESSES,
  PERSONAL_INFO,
  WISHLIST,
  HOME,
}
